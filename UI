-- UI Script Precision | Breakout (Sentenced)
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local RunService = game:GetService("RunService")

local Window = WindUI:CreateWindow({
    Title = "Precision Hub | Breakout (Sentenced)",
    SubTitle = "by 2knw | Version 0.0.1",
    Icon = "sword",
    Author = "2knw",
    Folder = "PrecisionBreakout",
})

-- ============ TABS ============
local MainTab = Window:Tab({ Title = "Main", Icon = "settings" })
local AimbotTab = Window:Tab({ Title = "Aimbot", Icon = "crosshair" })
local PlayersTab = Window:Tab({ Title = "Players", Icon = "user" })
local VisualsTab = Window:Tab({ Title = "Visuals", Icon = "eye" })
local TeleportsTab = Window:Tab({ Title = "Teleports", Icon = "map-pin" })

-- ============ MAIN TAB ============
MainTab:Divider()
MainTab:Label({ Title = "Player", Icon = "user" })

MainTab:Slider({
    Title = "Spinbot",
    Desc = "Spins your character at the chosen speed.",
    Min = 0,
    Max = 250,
    Default = 0,
    Callback = function(Value)
        SPINBOT_SPEED = Value
    end
})

-- ============ PLAYERS TAB ============
PlayersTab:Divider()
PlayersTab:Label({ Title = "Teleport to Player", Icon = "user" })

local playerOptions = {"Select a player..."}
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= player then
        table.insert(playerOptions, plr.Name)
    end
end

local playerDropdown = PlayersTab:Dropdown({
    Title = "Teleport to Player",
    Desc = "Select a player to teleport to them",
    Options = playerOptions,
    Default = "Select a player...",
    Callback = function(Value)
        if Value == "Select a player..." then return end
        local target = Players:FindFirstChild(Value)
        if target and target.Character then
            local hrp = target.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                local c = player.Character
                if c and c:FindFirstChild("HumanoidRootPart") then
                    c.HumanoidRootPart.CFrame = CFrame.new(hrp.Position + Vector3.new(0, 3, 0))
                end
            end
        end
    end
})

PlayersTab:Button({
    Title = "Update Players List",
    Callback = function()
        local options = {"Select a player..."}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(options, plr.Name)
            end
        end
        playerDropdown:SetOptions(options)
        playerDropdown:SetDefault("Select a player...")
    end
})

-- ============ VIEW PLAYER SECTION ============
PlayersTab:Divider()
PlayersTab:Label({ Title = "View Player", Icon = "eye" })

local viewPlayerOptions = {"Select a player..."}
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= player then
        table.insert(viewPlayerOptions, plr.Name)
    end
end

local SPECTATE_PLAYER = nil
local spectateConnection = nil

local viewPlayerDropdown = PlayersTab:Dropdown({
    Title = "View Player",
    Desc = "Select a player to spectate",
    Options = viewPlayerOptions,
    Default = "Select a player...",
    Callback = function(Value)
        if Value == "Select a player..." then return end
        SPECTATE_PLAYER = Value
    end
})

PlayersTab:Toggle({
    Title = "Spectate Player",
    Desc = "Toggle to spectate the selected player",
    Default = false,
    Callback = function(Value)
        if Value and SPECTATE_PLAYER then
            local target = Players:FindFirstChild(SPECTATE_PLAYER)
            if target and target.Character then
                workspace.CurrentCamera.CameraSubject = target.Character:FindFirstChildOfClass("Humanoid")
                spectateConnection = RunService.RenderStepped:Connect(function()
                    if target and target.Character then
                        local humanoid = target.Character:FindFirstChildOfClass("Humanoid")
                        if humanoid and humanoid.Health > 0 then
                            workspace.CurrentCamera.CameraSubject = humanoid
                        end
                    end
                end)
            end
        else
            if spectateConnection then
                spectateConnection:Disconnect()
                spectateConnection = nil
            end
            if player.Character then
                local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    workspace.CurrentCamera.CameraSubject = humanoid
                end
            end
        end
    end
})

PlayersTab:Button({
    Title = "Update Players List",
    Callback = function()
        local options = {"Select a player..."}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(options, plr.Name)
            end
        end
        viewPlayerDropdown:SetOptions(options)
        viewPlayerDropdown:SetDefault("Select a player...")
    end
})

-- ============ AIMBOT TAB ============
AimbotTab:Divider()
AimbotTab:Label({ Title = "Aimbot", Icon = "crosshair" })

AimbotTab:Toggle({
    Title = "Aimbot",
    Default = true,
    Callback = function(Value)
        AIMBOT_ENABLED = Value
        if _G.fovCircle then _G.fovCircle.Visible = Value end
        if not Value then game:GetService("UserInputService").MouseBehavior = Enum.MouseBehavior.Default end
    end
})

AimbotTab:Toggle({
    Title = "Wanted Only",
    Default = false,
    Callback = function(Value) WANTED_ONLY = Value end
})

AimbotTab:Toggle({
    Title = "Ignore Forcefield",
    Default = true,
    Callback = function(Value) IGNORE_FORCEFIELD = Value end
})

AimbotTab:Toggle({
    Title = "Ignore Friends",
    Default = true,
    Callback = function(Value) IGNORE_FRIENDS = Value end
})

AimbotTab:Toggle({
    Title = "Ignore Prisoners",
    Default = false,
    Callback = function(Value) IGNORE_PRISONERS = Value end
})

AimbotTab:Toggle({
    Title = "Ignore Guards",
    Default = false,
    Callback = function(Value) IGNORE_GUARDS = Value end
})

AimbotTab:Toggle({
    Title = "Ignore Escaped",
    Default = false,
    Callback = function(Value) IGNORE_ESCAPED = Value end
})

-- ============ TARGET ON SECTION ============
AimbotTab:Divider()
AimbotTab:Label({ Title = "Target On", Icon = "target" })

local targetOptions = {"None"}
for _, plr in ipairs(Players:GetPlayers()) do
    if plr ~= player then
        table.insert(targetOptions, plr.Name)
    end
end

local targetDropdown = AimbotTab:Dropdown({
    Title = "Target On",
    Desc = "Select a player to target",
    Options = targetOptions,
    Default = "None",
    Callback = function(Value)
        if Value == "None" then
            TARGET_ON_PLAYER = nil
        else
            TARGET_ON_PLAYER = Value
        end
    end
})

AimbotTab:Button({
    Title = "Reset Target",
    Callback = function()
        TARGET_ON_PLAYER = nil
        local newOptions = {"None"}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player then
                table.insert(newOptions, plr.Name)
            end
        end
        targetDropdown:SetOptions(newOptions)
        targetDropdown:SetDefault("None")
    end
})

AimbotTab:Slider({
    Title = "Smooth",
    Min = 1,
    Max = 100,
    Default = 80,
    Callback = function(Value) SMOOTHNESS = Value / 100 end
})

AimbotTab:Slider({
    Title = "Angle Offset",
    Min = 0,
    Max = 25,
    Default = 10.5,
    Callback = function(Value) AIM_ANGLE_OFFSET = Value end
})

-- ============ FOV SECTION ============
AimbotTab:Divider()
AimbotTab:Label({ Title = "FOV", Icon = "circle" })

AimbotTab:Dropdown({
    Title = "FOV Color",
    Desc = "Choose the FOV circle color",
    Options = {"Black", "Red", "Green", "Blue", "Yellow", "Purple", "Cyan", "Orange", "Pink", "White", "Rainbow"},
    Default = "Black",
    Callback = function(Value)
        setFOVColor(Value)
    end
})

AimbotTab:Toggle({
    Title = "Hide FOV Circle",
    Default = false,
    Callback = function(Value)
        HIDE_FOV_CIRCLE = Value
        if _G.toggleFOVCircle then
            _G.toggleFOVCircle(Value)
        end
    end
})

AimbotTab:Slider({
    Title = "FOV Size",
    Min = 30,
    Max = 500,
    Default = 100,
    Callback = function(Value)
        FOV_RADIUS = Value
        if _G.fovCircle then _G.fovCircle.Size = UDim2.new(0, Value*2, 0, Value*2) end
    end
})

-- ============ VISUALS TAB ============
VisualsTab:Divider()
VisualsTab:Label({ Title = "Visual Options", Icon = "eye" })

VisualsTab:Toggle({
    Title = "ESP",
    Default = false,
    Callback = function(Value)
        ESP_ENABLED = Value
        if _G.refreshESP then _G.refreshESP() end
    end
})

VisualsTab:Toggle({
    Title = "Hide ESP Names",
    Desc = "Hides the names above players when ESP is active",
    Default = false,
    Callback = function(Value)
        ESP_NAMES_ENABLED = not Value
        if _G.refreshESP then _G.refreshESP() end
    end
})

VisualsTab:Toggle({
    Title = "Hide Tag",
    Default = false,
    Callback = function(Value)
        HIDE_TAG_ENABLED = Value
        updateTagVisibility()
    end
})

VisualsTab:Divider()
VisualsTab:Label({ Title = "Customization", Icon = "paintbrush" })

VisualsTab:Toggle({
    Title = "Remove Fog",
    Desc = "Removes the fog from the game",
    Default = false,
    Callback = function(Value)
        if Value then
            game:GetService("Lighting").FogEnd = 100000
            game:GetService("Lighting").FogStart = 100000
        else
            game:GetService("Lighting").FogEnd = 5000
            game:GetService("Lighting").FogStart = 0
        end
    end
})

VisualsTab:Dropdown({
    Title = "Fog Color",
    Desc = "Choose the fog color",
    Options = {"Default", "Black", "Red", "Green", "Blue", "Yellow", "Purple", "Cyan", "Orange", "Pink", "White"},
    Default = "Default",
    Callback = function(Value)
        setFogColor(Value)
    end
})

VisualsTab:Dropdown({
    Title = "Skyboxes",
    Desc = "Choose your skybox",
    Options = {"None", "Gray Skybox", "2knw & Donk666 Skybox", "Snow Skybox", "Cartoon Skybox", "Lince Skybox", "Red Skybox", "Slam Skybox", "Green Eyeball Skybox", "LUK Skybox", "LUK Members Skybox"},
    Default = "None",
    Callback = function(Value)
        setSkybox(Value)
    end
})

-- ============ TELEPORTS TAB ============
TeleportsTab:Divider()
TeleportsTab:Label({ Title = "Teleports", Icon = "map-pin" })

local teleports = {
    {"Nexus", Vector3.new(116, 3, 245)},
    {"Hall 1", Vector3.new(201, 3, 106)},
    {"Hall 2", Vector3.new(79, 3, -12)},
    {"Cells", Vector3.new(-95, 21, 223)},
    {"Reception", Vector3.new(28, 3, 62)},
    {"Leaderboards", Vector3.new(90, 3, 55)},
    {"Cafeteria", Vector3.new(156, 3, 34)},
    {"Ventilation Duct", Vector3.new(140, 3, 3)},
    {"Kitchen", Vector3.new(244, 3, -82)},
    {"Armory", Vector3.new(209, 3, 152)},
    {"Camera Room", Vector3.new(212, 4, 120)},
    {"Yard", Vector3.new(74, 3, 351)},
    {"Yard Gym", Vector3.new(171, 3, 224)},
    {"Yard Tables", Vector3.new(142, 3, 363)},
    {"Basketball Court (Yard)", Vector3.new(-52, 3, 362)},
    {"Roof 1", Vector3.new(-66, 43, 183)},
    {"Roof 2", Vector3.new(214, 32, 183)},
    {"Roof 3", Vector3.new(203, 31, -91)},
    {"Helipad", Vector3.new(122, 55, -76)},
    {"Garage 1", Vector3.new(97, 3, -112)},
    {"Garage 2", Vector3.new(96, 31, -113)},
    {"Parking (Prison)", Vector3.new(24, 3, -36)},
    {"Gate", Vector3.new(-196, 3, -28)},
    {"Gate Room", Vector3.new(-200, 3, -84)},
    {"Parking 1 (City)", Vector3.new(-507, -26, 383)},
    {"Parking 2 (City)", Vector3.new(-1604, -34, 995)},
    {"Market", Vector3.new(-874, -34, 836)},
    {"Gas Station", Vector3.new(-1139, -27, 997)},
    {"Escaped Base", Vector3.new(-1575, -30, 796)},
    {"Under the Map", Vector3.new(35, -34, 26)},
    {"Secret Guard Room", Vector3.new(3, -8, 243)},
}

for _, tp in ipairs(teleports) do
    TeleportsTab:Button({
        Title = tp[1],
        Callback = function()
            local c = player.Character
            if c and c:FindFirstChild("HumanoidRootPart") then
                c.HumanoidRootPart.CFrame = CFrame.new(tp[2])
            end
        end
    })
end
